# Silly Game 2.2.1 修复说明

基于用户提供的 Silly-Game-v2.2.0-companion-pinned-launcher-persistence.zip 修改。

## 修改内容

- 修复主题样式对所有 span、strong、b 等元素强制着色的问题，恢复蜘蛛纸牌花色、UNO 数字及斗地主牌面的独立配色。
- 斗地主使用不透明白色牌面，明确区分红色花色、黑色花色和王牌；UNO 中央数字使用较深的牌色，提高对比度。
- 收纳把手支持鼠标与触摸拖动，分别记住悬浮球和把手的位置。拖动结束不会误打开游戏，点击仍可打开且保持收纳状态。
- 收纳把手宽度从 18px 调整到 28px，补充键盘焦点提示与正确的操作文案。
- 窗口缩放时重新限制悬浮球及把手的位置；“恢复默认位置”同时重置两者。
- 合并两种悬浮控件的拖动逻辑，处理多指干扰、指针取消和捕获丢失；拖动期间仅更新坐标，结束时保存。
- 修复 UNO 角色请求失败、第二次请求返回和后续定时器可能在离开旧牌局后继续操作的问题。
- UNO 重绘、重新开始后清除旧选色状态，规则入口拒绝过期索引、非当前回合、已结束牌局和非法选色。
- UNO 摸牌堆与工具栏按钮同步禁用状态，结束后不再显示可出牌高亮。
- UNO 卡牌内容改用文本节点生成，避免将存档文本作为 HTML 解释。
- 斗地主手牌溢出时保留左侧起点可滚动访问；补充 hidden 属性的显示规则，避免选色面板被布局样式强制显示。
- 重复点击游戏入口不再重建已经打开的界面。

## 安装

1. 备份现有扩展目录。
2. 将压缩包内的 index.js、style.css、manifest.json 和 README.md 覆盖到原 Silly Game 扩展目录，保持原目录名。
3. 刷新 SillyTavern 页面；如仍载入旧样式，使用强制刷新。

原有游戏存档键、角色陪玩设置及悬浮球收纳设置保持不变。新增收纳把手位置键 stgc-restore-position-v1。

## 验证范围

- JavaScript 语法检查通过；版本号统一为 2.2.1。
- 21 项 Node VM / DOM 模拟回归通过，覆盖 UNO 基本规则、异步失效、选色重置、禁用状态、文本渲染及悬浮控件拖动。
- 回归脚本位于 tests/regression.cjs，可在安装 Node.js 后于解压目录运行：node tests/regression.cjs。
- 当前环境启动 Edge 时出现系统权限错误，未完成真实浏览器截图与布局验收。DOM 模拟测试不能替代浏览器渲染测试。
- 未连接真实 SillyTavern、角色卡或陪玩 API；未逐一完整通关所有小游戏。

建议在实际酒馆中复核：白天/夜间/跟随主题下的三种卡牌游戏，手机长手牌的左右滚动，收纳把手拖动后刷新与横竖屏切换，以及请求过程中退出/重新开局。
