# Silly Game 2.2.3 修复说明

基于此前的 2.2.2 修复版继续修改。保留原有游戏存档键、角色配置、UNO 导航与模式修复，以及收纳把手拖动功能。

## 蜘蛛纸牌

- 根因：卡牌绝对定位，牌列只有固定最小高度，后续卡牌不参与外层布局计算，长牌堆会溢出并覆盖底部内容。
- 每列根据实际牌数预留完整高度，卡牌垂直露出区域增至 32px；发牌后同步更新。
- 发牌按钮移到牌桌上方的独立操作栏，不再随牌桌横向滚动；按钮至少 44px 高。
- 存在空列而不能发牌时，明确显示原因。
- 修复重新开始、切换难度后计时器仍检查旧牌局引用而停止更新的问题。

## 角色陪玩与通用操作

- 删除旧版高优先级的强制透明背景规则。夜间使用 #151515 实色面板及浅色文字，白天使用白色面板及深色文字。
- 内部设置区使用主题表面色，并修复角色名称、部分状态文字及下拉选项与强制主题不一致的问题。
- 角色名称和世界书条目名称关联原生复选框，点击名称即可勾选；头像按钮仍独立负责换头像。
- 移除向 DOM append(null) 导致非当前角色行出现字面量“null”的问题。
- 扩大页头、游戏工具栏、状态栏及世界书按钮点击区域，增加焦点提示；状态栏按钮可换行，长模式名不会无限撑宽。

## 软糖视觉与三消逻辑

- 替换旧软糖 CSS 段，而非继续叠加旧规则。保留六种元素：粉色星星、黄色包裹糖、橙色熊熊、红色心形、蓝色糖环、绿色果冻。
- 重新绘制糖体、高光和细微立体阴影。清晰的轮廓与固定配色共同区分元素。
- 特殊糖角标、果冻目标虚线、冰块、藤蔓改成独立图层，不再争用同一个伪元素；支持同时显示多个状态。
- 选中状态增加紫色轮廓，糖格附带名称与状态的辅助阅读标签，装饰图层不拦截点击。
- 道具采用至少 44px 高的按钮；窄屏工具栏分为两列。
- 通关遮罩定位在游戏内容区内，避免覆盖外部返回/关闭页头；弹窗自身使用实色背景。
- 修复存档规范化丢失果冻目标标记的问题，保存额外增加的最大步数。
- “洗牌”和“+5 步”点击后立即执行并保存，不再要求再点击棋盘。
- 锤子、清色道具共用障碍清除和目标计数逻辑，避免清掉障碍却不增加目标进度；道具操作后正常处理连锁。

## 安装与预览

备份现有扩展目录，将包内 index.js、style.css、manifest.json、README.md 覆盖到原目录，保持原目录名，然后强制刷新酒馆页面。

另附 UI预览-2.2.3.html。双击用浏览器打开，可切换明暗主题和三种示例页面；预览引用与修复包相同的 CSS，不访问网络，不读写实际游戏存档。预览中的发牌和选中仅为布局演示。

如果旧版本已经把某份存档的果冻标记清空，新版无法还原它们原先的位置；遇到这种旧存档，需要重开当前关卡。

## 验证范围

- JavaScript 语法检查、预览页脚本语法检查通过。
- 36 项 Node VM / DOM 模拟回归通过，包含上一版的返回、关闭、重新打开、模式保持、异步失效、悬浮控件等测试，以及本次蜘蛛布局状态、计时器、三消存档、道具和图层测试。
- 静态 CSS 检查通过：规则块配对、明暗模式陪玩面板最终背景/文字、角色名点击高度、通关遮罩定位、软糖规则去重。静态检查只覆盖指定选择器，不能代替浏览器布局引擎。
- 对 2.2.2 运行新增检查，复现了透明背景、蜘蛛停表、果冻标记丢失；对应检查在 2.2.3 通过。
- 当前环境无法启动真实浏览器（此前 Edge / Playwright 启动权限错误），未完成截图、实际触屏命中或真实酒馆 API 联调，也未完整通关全部小游戏。

测试脚本位于 tests/。安装 Node.js 后，在解压目录运行 node tests/regression.cjs 和 node tests/css-regression.cjs。
