# Silly Game 2.2.4 修复说明

在 2.2.3 基础上继续修改，保留此前 UNO 返回与模式保持、夜间卡牌与陪玩实色背景、蜘蛛发牌布局和计时器、软糖样式与三消存档/道具等修复。

## 收纳把手

- 去掉显眼的字母与阴影，改为低透明度的细小把手，悬停或拖动时变清晰。
- 完整尺寸为 18×40px，贴边时部分藏入屏幕边缘，只露出约 12px。
- 可拖动，松开后吸附最近的左右边缘；保存高度和侧边，窗口变化后仍保持所在侧。

## 自定义名字和头像

- 打开“角色陪玩”，展开“我的名字与头像”，可设置自己的资料。
- 在对应普通角色或世界书条目下展开“编辑名字与头像”，可分别设置该角色的资料。
- 输入名字、选择头像后，点击保存按钮。名字和头像保存在当前浏览器、当前站点的本地存储中；刷新后保留。
- 每个资料编辑器都可以单独恢复默认，不会重置其他角色。
- 修改仅作用于小游戏内显示，不改写酒馆原角色卡。
- 上传图片沿用压缩处理；处理期间禁止保存，避免保存旧图；存储失败会明确提示，不会假报已保存。
- 修复未选择任何陪玩角色时，加载配置异常导致个人设置回退默认值的问题。

## 牌桌和棋局头像

- UNO 和斗地主的陪玩头像桌面约 84×104px，窄屏约 68×86px；角色名字同时显示。
- 棋类角色头像桌面约 96×120px，窄屏约 80×100px，身份栏位于棋盘上方。
- 自己的名字与头像显示在相应玩家身份区。
- UNO 对手、中央牌区、选色区分别占用布局行；斗地主座位与出牌区按正常布局排列，减少绝对定位遮挡。
- 台词放入正常布局，不再悬浮覆盖牌面或棋盘；长台词最多显示三行。

## 安装与预览

备份现有扩展目录，将包内 index.js、style.css、manifest.json、README.md 覆盖到原目录，保持目录名，然后强制刷新酒馆页面。

另附 UI预览-2.2.4.html，使用修复包相同的 CSS，可切换明暗主题和 UNO、斗地主、棋类示例。示例头像仅用于展示尺寸，预览不读取或修改实际设置及游戏存档。

## 验证范围

- JavaScript 与预览页脚本语法检查通过。
- 43 项 Node VM / DOM 模拟测试通过，包括此前回归和本次个人资料、普通角色/世界书独立资料、编辑器保存与恢复、存储失败、UNO 身份区、棋盘前身份栏、把手吸边与窗口变化等检查。
- 针对性静态 CSS 检查通过，涵盖明暗主题、头像桌面/窄屏尺寸、把手尺寸和关键区域定位。静态检查不能替代浏览器布局引擎。
- 当前环境启动真实浏览器受权限限制，未完成真实酒馆中的截图、触屏点击、图片解码与 API 联调，也未完整通关全部小游戏。
- 包内 tests/ 提供测试脚本及结果；可在解压目录运行 node tests/regression.cjs 和 node tests/css-regression.cjs。
